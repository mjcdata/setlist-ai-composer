# Setlist AI Composer

## Overview

Setlist AI Composer is an AI powered system that generates performance setlists and keeps them synchronized with a matching YouTube playlist. Users can add, remove, or replace songs via Discord commands, and updates are automatically reflected in both the setlist document and playlist in real time.

---

## Key Features

* Automatic YouTube playlist creation and syncing
* Discord command-based control system
* Real-time updates across setlist and playlist
* Simple workflow for live performance management

---

## How It Works

1. User sends a command in Discord (e.g., add, remove, replace song)
2. System interprets the request using AI
3. Setlist is updated accordingly in Google Docs
4. YouTube playlist is automatically updated to match
5. Updated setlist document is regenerated and synced

---

## Example Commands

* Add a song: `add "Song Name"`
* Remove a song: `remove "Song Name"`
* Replace a song: `replace "Old Song" with "New Song"`

---

## Goals

* Maintain perfect sync between setlist and playlist
* Reduce manual setlist management for live performers
* Enable fast iteration during rehearsals and performances

---

## Future Improvements

* Performance notes per song (key, tempo, arrangement notes)
* Smart setlist ordering based on energy flow
* Collaboration between multiple band members
* Sharing setlists with external collaborators
* Setlist templates for different performance types

---

## Purpose

Built for live musicians who want a fast, flexible, and intelligent way to build and manage performance setlists without manual playlist and document upkeep.
